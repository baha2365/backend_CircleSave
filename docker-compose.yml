version: '3.9'

networks:
  baha2365-backend-circlesave-net:
    driver: bridge
    name: baha2365-backend-circlesave-net  # explicit name forces creation first

services:
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - baha2365-backend-circlesave-net
    restart: unless-stopped

  backend:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      DATABASE_URL: postgresql://postgres:123@postgres:5432/CircleSave
      REDIS_URL: redis://redis:6379
      NODE_ENV: production
      PORT: 3000
      JWT_ACCESS_SECRET: super_secret_access_key_very_long_123456
      JWT_REFRESH_SECRET: super_secret_refresh_key_very_long_654321
      JWT_ACCESS_EXPIRES_IN: 15m
      JWT_REFRESH_EXPIRES_IN: 7d
      API_VERSION: v1
      CORS_ORIGINS: http://localhost,https://baha2365-backend-circlesave-frontend.kazi.rocks
      FRONTEND_URL: https://baha2365-backend-circlesave-frontend.kazi.rocks
    # removed bind mount — no local filesystem on the server
    command: sh -c "npx prisma generate && npx prisma migrate deploy && node src/server.js"
    networks:
      - baha2365-backend-circlesave-net
    restart: unless-stopped

  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: CircleSave
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d CircleSave"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - baha2365-backend-circlesave-net

  redis:
    image: redis:7
    restart: always
    networks:
      - baha2365-backend-circlesave-net

volumes:
  pgdata: